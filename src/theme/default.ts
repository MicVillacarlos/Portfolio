const id = 1;

const color = {
  primary: "#f4bd0b",
  secondary: '#000000',
};

const font = {
  primary: "Syncopate",
  secondary: "Gruppo",
};

const defaultTheme = {
  color,
  font,
  id,
};

export default defaultTheme;
